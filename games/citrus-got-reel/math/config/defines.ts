/**
 * Defines.ts
 *
 * Collected enums and defines that don't need separate files
 */

export enum ActionType {
	Main = "main",
	Ante = "ante",
	BonusBuy = "bonusbuy",
}

export enum FeatureType {
	Wild = "Wild",
	DirectionalWild = "DirectionalWild",
	CollectorWild = "CollectorWild",
	PayerWild = "PayerWild",
}

export enum SpecialModeType {
	None = "None",
	BonusBuySpin = "BonusBuySpin",
}
